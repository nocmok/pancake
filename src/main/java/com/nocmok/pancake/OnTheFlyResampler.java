package com.nocmok.pancake;

/**
 * This is just marker interface, which indicates, that resampler will produce
 * vrt dataset, instead of creating full size resampled image.
 */
public interface OnTheFlyResampler extends Resampler {

}
